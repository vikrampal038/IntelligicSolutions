import React from "react";
import GameFile from "@/components/Game/GameFile";

const Game = () => {
  return (
    <div>
      <section
        section
        id="hero"
        className="bg-gradient-brand-50-100-200">
        <GameFile />
      </section>
    </div>
  );
};

export default Game;
